# 1. imoc_checkin_user [考勤人员打卡记录表]
> 记录人员的每一次打卡记录，不负责考勤的绩效统计，只负责记录打卡情况

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_checkin_user_id_seq') | 主键 |
| userid | int4 | 否 | 是 |  | 用户唯一ID，对应"用户表"的ID |
| project_id | int4 | 否 | 是 |  | 排班项目唯一ID，对应"考勤排班项目表"的ID |
| duty_id | int4 | 否 | 是 |  | 排班唯一ID，对应"考勤排班表"的ID |
| class_id | int4 | 否 | 是 |  | 班次唯一ID，对应"考勤班次表"的ID |
| range_id | int4 | 否 | 是 |  | 时间段唯一ID，对应"考勤班次时段表"的ID |
| related_id | int4 | 否 | 是 |  | 人员排班记录唯一ID，对应"考勤人员排班记录表"的ID |
| type | VARCHAR(255) | 否 | 是 |  | 类型：up=上班, down=下班 |
| range_start_time | VARCHAR(255) | 否 | 是 |  | 考勤上班时间，format: %H:%M |
| range_end_time | VARCHAR(255) | 否 | 是 |  | 考勤下班时间，format: %H:%M |
| tertian | int2 | 否 | 是 |  | 是否隔日：1=是，0=否 |
| duty_date | date | 否 | 是 |  | 值班日期，format: %Y-%m-%d |
| duty_location | VARCHAR(255) | 否 | 是 |  | 值班打卡位置 |
| location | VARCHAR(255) | 否 | 是 |  | 实际打卡位置 |
| lng | VARCHAR(255) | 否 | 是 |  | 经纬度 |
| lat | VARCHAR(255) | 否 | 是 |  | 经纬度 |
| status | VARCHAR(255) | 否 | 是 |  | 打卡状态，英文。直接看表"考勤人员排班记录表"的`status`字段获取考勤状态 |
| create_time | timestamp | 否 | 是 |  | 创建时间 |
| update_time | timestamp | 否 | 是 |  | 更新时间 |
| is_del | int2 | 否 | 是 |  | 是否删除 |
| is_manual | int2 | 否 | 否 | 0 | 是否手动签到 |

<br>

# 2. imoc_class [考勤班次表]
> 记录配置好的考勤班次。基本每个运维项目需要人员驻点，该表即负责记录配置的每个驻点班次

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_id_seq') | 班次唯一ID |
| name | VARCHAR(255) | 否 | 是 |  | 班次名称 |
| endure_minutes | VARCHAR(255) | 否 | 是 |  | 允许多早/多晚打卡的弹性时间，单位：分钟 |
| info | VARCHAR(255) | 否 | 是 |  | 班次说明 |
| create_time | timestamp | 否 | 是 |  | 创建时间 |
| create_userid | int4 | 否 | 是 |  | 创建用户唯一ID |
| update_time | timestamp | 否 | 是 |  | 更新时间 |
| update_userid | int4 | 否 | 是 |  | 更新用户唯一ID |
| is_del | int2 | 否 | 是 |  | 是否删除：1=是，0=否 |

<br>

# 3. imoc_class_duty [考勤排班表]
> 记录关联排班项目、班次的较具体的考勤排班表，给到具体开始、结束时间，值班方式等
注：每个排班项目可以有多个排班

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_duty_id_seq') | 排班唯一ID |
| project_id | int4 | 否 | 是 |  | 排班项目唯一ID，对应"考勤排班项目表"的ID |
| class_id | int4 | 否 | 否 |  | 班次唯一ID，对应"考勤班次表"的ID |
| start_date | date | 否 | 否 |  | 排班开始日期，format: %Y-%m-%d |
| end_date | date | 否 | 否 |  | 排班结束日期，format: %Y-%m-%d |
| type | VARCHAR(255) | 否 | 否 |  | 值班方式，如：白班、夜班、轮班等 |
| remark | VARCHAR(255) | 否 | 否 |  | 排班备注说明 |
| status | VARCHAR(255) | 否 | 否 |  | 排班状态 |
| create_time | timestamp | 否 | 否 |  | 创建时间 |
| create_userid | int4 | 否 | 否 |  | 创建用户唯一ID |
| update_time | timestamp | 否 | 否 |  | 更新时间 |
| update_userid | int4 | 否 | 否 |  | 更新用户唯一ID |
| is_del | int2 | 否 | 否 |  | 是否删除：1=是，0=否 |

<br>

# 4. imoc_class_duty_user [考勤人员排班记录表]
> 最具体的统计绩效的考勤表，与"考勤人员打卡记录表"的区别是记录了考勤时间范围，具体考勤状态。

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_duty_user_id_seq') | 人员排班记录唯一ID |
| userid | int4 | 否 | 否 |  | 用户唯一ID，对应"用户表"的ID |
| project_id | int4 | 否 | 否 |  | 排班项目唯一ID，对应"考勤排班项目表"的ID |
| duty_id | int4 | 否 | 否 |  | 排班唯一ID，对应"考勤排班表"的ID |
| class_id | int4 | 否 | 否 |  | 班次唯一ID，对应"考勤班次表"的ID |
| team_id | int4 | 否 | 否 |  | 团队唯一ID，对应"考勤团队表"的ID |
| duty_date | date | 否 | 否 |  | 值班日期，format: %Y-%m-%d |
| duty_time | TEXT | 否 | 否 |  | 应值班时间范围 |
| duty_status | TEXT | 否 | 否 |  | 考勤状态，英文。不看这个字段，看`status` |
| checkin_time | TEXT | 否 | 否 |  | 实际签到/签退时间 |
| checkin_location | TEXT | 否 | 否 |  | 实际签到/签退位置 |
| type | VARCHAR(255) | 否 | 否 |  | 值班类型 |
| status | VARCHAR(255) | 否 | 否 |  | 考勤状态，中文，选项：未到岗，早退，缺勤，未签到，正常，迟到早退，迟到，未签退 |
| create_time | timestamp | 否 | 否 |  | 创建时间 |
| update_time | timestamp | 否 | 否 |  | 更新时间 |
| is_del | int2 | 否 | 否 |  | 是否删除：1=是，0=否 |

<br>

# 5. imoc_class_project [考勤排班项目表]
> 记录排班项目表。排班项目仅用于规划排班，不是工程项目，
但有关联的（工程）项目，对应字段"belong_project_id"

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_project_id_seq') | 排班项目唯一ID |
| name | VARCHAR(255) | 否 | 是 |  | 排班项目名称 |
| is_insance | int2 | 否 | 是 |  | 是否重保项目：1=是，0=否 |
| create_time | timestamp | 否 | 是 |  | 创建时间 |
| create_userid | int4 | 否 | 是 |  | 创建用户唯一ID |
| update_time | timestamp | 否 | 是 |  | 更新时间 |
| update_userid | int4 | 否 | 是 |  | 更新用户唯一ID |
| is_del | int2 | 否 | 是 |  | 是否删除：1=是，0=否 |
| status | VARCHAR(255) | 否 | 是 |  | 排班项目状态，选项：进行中，未开始，已结束 |
| belong_project_id | int4 | 否 | 否 |  | 所属项目唯一ID，对应"项目表"的ID |

<br>

# 6. imoc_class_range [考勤班次时段表]
> 记录配置的班次时段

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_range_id_seq') | 班次时段唯一ID |
| class_id | int4 | 否 | 是 |  | 班次唯一ID，对应"考勤班次表"的ID |
| start_time | VARCHAR(255) | 否 | 是 |  | 时段开始时间，format: %H:%M |
| end_time | VARCHAR(255) | 否 | 是 |  | 时段结束时间，format: %H:%M |
| tertian | int2 | 否 | 是 |  | 是否隔日：1=是，0=否 |

<br>

# 7. imoc_class_team [考勤团队表]
> 记录考勤团队，但用户与排班项目与班次与排班都属于多对多关系

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_team_id_seq') | 团队唯一ID |
| user_id | int4 | 否 | 否 |  | 用户唯一ID，对应"用户表"的ID |
| project_id | int4 | 否 | 否 |  | 排班项目唯一ID，对应"考勤排班项目表"的ID |
| class_id | int4 | 否 | 否 |  | 班次唯一ID，对应"考勤班次表"的ID |
| duty_id | int4 | 否 | 否 |  | 排班唯一ID，对应"考勤排班表"的ID |

<br>

# 8. imoc_class_user [考勤人员记录表]
> 记录考勤人员信息，与更新的考勤人员信息。
出于更新需要，主键与用户并非唯一对应，用户ID才与用户一一对应

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_user_id_seq') | 主键 |
| userid | int4 | 否 | 否 |  | 用户唯一ID，对应"用户表"的ID |
| groupid | int4 | 否 | 否 |  | 所属单位唯一ID，对应"所属单位表"的ID |
| type | VARCHAR(255) | 否 | 否 |  | 人员类型，选项：运维人员，行政人员 |
| mobile | VARCHAR(255) | 否 | 否 |  | 手机号码 |
| location | TEXT | 否 | 否 |  | 常驻打卡位置 |
| create_time | timestamp | 否 | 否 |  | 创建时间 |
| create_userid | int4 | 否 | 否 |  | 创建用户唯一ID |
| update_time | timestamp | 否 | 否 |  | 更新时间 |
| update_userid | int4 | 否 | 否 |  | 更新用户唯一ID |
| is_del | int2 | 否 | 否 |  | 是否删除：1=是，0=否 |
| name | VARCHAR(255) | 否 | 否 |  | 人员姓名 |

<br>

# 9. imoc_class_appeal [考勤申诉表]
> 异常考勤可以申请申诉，申诉通过后可以订正为正常状态

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | serial | 是 | 是 | nextval('imoc_class_appeal_id_seq') | 主键 |
| duty_id | int4 | 否 | 否 |  | 排班唯一ID，对应"考勤排班表"的ID |
| type | VARCHAR(255) | 否 | 否 |  | 申诉类型，唯一值：appeal |
| start_time | TIMESTAMP | 否 | 否 |  | 申诉开始时间 |
| end_time | TIMESTAMP | 否 | 否 |  | 申诉结束时间 |
| reson | VARCHAR(255) | 否 | 否 |  | 申诉原因 |
| pic_url | TEXT | 否 | 否 |  | 申诉附件图片地址 |
| status | VARCHAR(255) | 否 | 否 |  | 申诉状态 |
| reject_reson | VARCHAR(255) | 否 | 否 |  | 审批拒绝理由 |
| gov_reject_reson | VARCHAR(255) | 否 | 否 |  | 业主审批拒绝理由 |
| create_userid | int4 | 否 | 否 |  | 申请人用户唯一ID，对应"用户表"的ID |
| create_time | TIMESTAMP | 否 | 否 |  | 申请时间 |
| update_time | TIMESTAMP | 否 | 否 |  | 更新时间 |
| approve_user | VARCHAR(255) | 否 | 否 |  | 审批人员姓名 |
| approve_userid | int4 | 否 | 否 |  | 审批人用户唯一ID，对应"用户表"的ID |
| approve_time | TIMESTAMP | 否 | 否 |  | 审批时间 |
| gov_approve_user | VARCHAR(255) | 否 | 否 |  | 业主审批人员姓名 |
| gov_approve_userid | int4 | 否 | 否 |  | 业主审批人用户唯一ID，对应"用户表"的ID |
| gov_approve_time | TIMESTAMP | 否 | 否 |  | 业主审批时间 |
| is_del | int2 | 否 | 否 | 0 | 是否删除：1=是，0=否 |

<br>

# 10. lw_management_project [项目表]
> 记录项目（并非排班项目），唯一项目ID查询项目名称

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | SERIAL | 是 | 是 |  | 项目唯一ID |
| name | varchar(255) | 否 | 否 |  | 项目名称 |

<br>

# 11. lwops_auth_users [用户表]
> 记录用户，唯一用户ID查询用户姓名

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | SERIAL | 是 | 是 |  | 用户唯一ID |
| name | varchar(100) | 否 | 是 | ''::character varying | 用户姓名 |

<br>

# 12. lwops_org_groups [所属单位表]
> 记录单位，唯一单位ID查询单位名称

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| id | SERIAL | 是 | 是 |  | 单位唯一ID |
| name | varchar(32) | 否 | 是 |  | 单位名称 |

<br>

# 13. imoc_attendance_all [人员考勤记录表]
> 记录人员天打卡记录。是视图，并非表。

## 表结构

| 字段名 | 数据类型 | 主键 | 非空 | 默认值 | 备注说明 |
|--------|----------|------|------|--------|----------|
| 用户名称 | VARCHAR(100) | 否 | 是 |  | 用户姓名 |
| 单位名称 | VARCHAR(100) | 否 | 是 |  | 单位名称 |
| 项目名称 | VARCHAR(255) | 否 | 是 |  | 项目名称 |
| 排班名称 | VARCHAR(255) | 否 | 是 |  | 排班名称 |
| 班次名称 | VARCHAR(255) | 否 | 是 |  | 班次名称 |
| 值班日期 | date | 否 | 是 |  | 值班日期 |
| 值班时间 | TEXT | 否 | 是 |  | 值班时间 |
| 打卡时间 | TEXT  | 否 | 是 |  | 打卡时间 |
| 考勤状态 | VARCHAR(255) | 否 | 是 |  | 考勤状态，选项：未到岗，早退，缺勤，未签到，正常，迟到早退，迟到，未签退。而考勤异常状态：早退、未签退、缺勤、迟到|
| 是否申诉 | VARCHAR(16) | 否 | 是 |  | 是否申诉 |
| 是否请假 | VARCHAR(16) | 否 | 是 |  | 是否请假 |